---
'@icebreakers/commitlint-config': patch
'@icebreakers/eslint-config': patch
'@icebreakers/stylelint-config': patch
---

Export explicit public config result types for the `icebreaker` factory helpers.

Also expose the Stylelint config input types from the package entry so consumers
can reference both input and output config types directly.
